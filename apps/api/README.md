# API

This package contains the NestJS API.

## Development

From the repo root:

```bash
pnpm run dev
```

To run only the API server after the DB package has been built:

```bash
pnpm --filter api run dev
```
