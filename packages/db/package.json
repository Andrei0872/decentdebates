{
  "name": "@decentdebates/db",
  "version": "0.0.1",
  "private": true,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:clean": "rimraf dist && tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "seed": "tsc -p tsconfig.json && knex --knexfile dist/knexfile.js seed:run",
    "seed:file": "tsc -p tsconfig.json && knex --knexfile dist/knexfile.js seed:run --specific"
  },
  "dependencies": {
    "@nestjs/common": "9.0.0",
    "@nestjs/config": "^2.2.0",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/node": "^16.0.0",
    "knex": "^2.4.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.7.4"
  }
}
